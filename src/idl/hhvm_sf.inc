// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"is_hhvm_func", T(Void), S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.is-hhvm-func.php )\n *\n * the function is used for vertify the programming whether is hhvm or php\n *\n * @return     mixed   there is no need to return. because the function is\n *                     affetc as vertify\n */", 

#elif EXT_TYPE == 1

#elif EXT_TYPE == 2

#endif
