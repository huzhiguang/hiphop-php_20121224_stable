// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"mssql_connect", T(Variant), S(0), "server", T(String), "N;", S(2), "null", S(0), "username", T(String), "N;", S(2), "null", S(0), "password", T(String), "N;", S(2), "null", S(0), "new_link", T(Boolean), "b:0;", S(4), "false", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-connect.php )\n *\n * Establishes a connection to a MS SQL server\n *\n * @server     string  The MS SQL server. It can also include a port\n *                     number, e.g. hostname:port (Linux), or hostname,port\n *                     (Windows).\n * @username   string  The username. Default value is defined by\n *                     mysql.default_user. In SQL safe mode, this parameter\n *                     is ignored and the name of the user that owns the\n *                     server process is used.\n * @password   string  The password. Default value is defined by\n *                     mysql.default_password. In SQL safe mode, this\n *                     parameter is ignored and empty password is used.\n * @new_link   bool    If a second call is made to mysql_connect() with the\n *                     same arguments, no new link will be established, but\n *                     instead, the link identifier of the already opened\n *                     link will be returned. The new_link parameter\n *                     modifies this behavior and makes mysql_connect()\n *                     always open a new link, even if mysql_connect() was\n *                     called before with the same parameters. In SQL safe\n *                     mode, this parameter is ignored.\n *\n * @return     mixed   Returns a MS SQL link identifier on success, or\n *                     FALSE on error.\n */", 
"mssql_pconnect", T(Variant), S(0), "server", T(String), "N;", S(2), "null", S(0), "username", T(String), "N;", S(2), "null", S(0), "password", T(String), "N;", S(2), "null", S(0), "new_link", T(Boolean), "b:0;", S(4), "false", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-pconnect.php )\n *\n * First, when connecting, the function would first try to find a\n * (persistent) link that's already open with the same host, username and\n * password. If one is found, an identifier for it will be returned instead\n * of opening a new connection.\n *\n * @server     string  The MS SQL server. It can also include a port\n *                     number, e.g. hostname:port (Linux), or hostname,port\n *                     (Windows).\n * @username   string  The username. Default value is defined by\n *                     mysql.default_user. In SQL safe mode, this parameter\n *                     is ignored and the name of the user that owns the\n *                     server process is used.\n * @password   string  The password. Default value is defined by\n *                     mysql.default_password. In SQL safe mode, this\n *                     parameter is ignored and empty password is used.\n * @new_link   bool    If a second call is made to mysql_connect() with the\n *                     same arguments, no new link will be established, but\n *                     instead, the link identifier of the already opened\n *                     link will be returned. The new_link parameter\n *                     modifies this behavior and makes mysql_connect()\n *                     always open a new link, even if mysql_connect() was\n *                     called before with the same parameters. In SQL safe\n *                     mode, this parameter is ignored.\n *\n * @return     mixed   Returns a MS SQL link identifier on success, or\n *                     FALSE on error.\n */", 
"mssql_close", T(Boolean), S(0), "link_identifier", T(Variant), "N;", S(2), "null", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-close.php )\n *\n * Closes the link to a MS SQL Server database that's associated with the\n * specified link identifier. If the link identifier isn't specified, the\n * last opened link is assumed.\n *\n * @link_identifier\n *             mixed   The MSSQL connection. If the link identifier is not\n *                     specified, the last link opened by mysql_connect()\n *                     is assumed. If no such link is found, it will try to\n *                     create one as if mysql_connect() was called with no\n *                     arguments. If no connection is found or established,\n *                     an E_WARNING level error is generated.\n *\n * @return     bool    Returns TRUE on success or FALSE on failure.\n */", 
"mssql_select_db", T(Boolean), S(0), "database_name", T(String), "N;", S(2), "null", S(0), "link_identifier", T(Variant), "N;", S(2), "null", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-select-db.php )\n *\n * sets the current active database on the server that's associated with\n * the specified link identifier.\n *\n * @database_name\n *             string\n * @link_identifier\n *             mixed   The MSSQL connection. If the link identifier is not\n *                     specified, the last link opened by mysql_connect()\n *                     is assumed. If no such link is found, it will try to\n *                     create one as if mysql_connect() was called with no\n *                     arguments. If no connection is found or established,\n *                     an E_WARNING level error is generated.\n *\n * @return     bool    Returns TRUE on success or FALSE on failure.\n */", 
"mssql_query", T(Variant), S(0), "query", T(String), "N;", S(2), "null", S(0), "link_identifier", T(Variant), "N;", S(2), "null", S(0), "batch_size", T(Int64), "i:0;", S(4), "0", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-query.php )\n *\n * sends a query to the currently active database on the server that's\n * associated with the specified link identifier\n *\n * @query      string  An SQL query.\n * @link_identifier\n *             mixed   The MSSQL connection. If the link identifier is not\n *                     specified, the last link opened by mysql_connect()\n *                     is assumed. If no such link is found, it will try to\n *                     create one as if mysql_connect() was called with no\n *                     arguments. If no connection is found or established,\n *                     an E_WARNING level error is generated.\n * @batch_size int     An SQL query.\n *\n * @return     mixed   Returns a MS SQL result resource on success, TRUE if\n *                     no rows were returned, or FALSE on error\n */", 
"mssql_num_rows", T(Variant), S(0), "result", T(Variant), "N;", S(2), "null", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-num-rows.php )\n *\n * returns the number of rows in a result set\n *\n * @result     mixed   The result resource that is being evaluated. This\n *                     result comes from a call to mssql_query().\n *\n * @return     mixed   Returns the number of rows, as an integer.\n */", 
"mssql_rows_affected", T(Variant), S(0), "result", T(Variant), "N;", S(2), "null", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-rows-affected.php\n * )\n *\n * Returns the number of records affected by the last write query.\n *\n * @result     mixed   A MS SQL link identifier, returned by\n *                     mssql_connect() or mssql_pconnect()\n *\n * @return     mixed   Returns the number of records affected by last\n *                     operation.\n */", 
"mssql_fetch_row", T(Variant), S(0), "result", T(Variant), "N;", S(2), "null", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-fetch-row.php )\n *\n * fetches one row of data from the result associated with the specified\n * result identifier. The row is returned as an array. Each result column\n * is stored in an array offset, starting at offset 0.Subsequent call to\n * mssql_fetch_row() would return the next row in the result set, or FALSE\n * if there are no more rows.\n *\n * @result     mixed   The result resource that is being evaluated. This\n *                     result comes from a call to mssql_query().\n *\n * @return     mixed   Returns an array that corresponds to the fetched\n *                     row, or FALSE if there are no more rows.\n */", 
"mssql_fetch_assoc", T(Variant), S(0), "result", T(Variant), "N;", S(2), "null", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-fetch-assoc.php )\n *\n * Returns an associative array that corresponds to the fetched row and\n * moves the internal data pointer ahead. mssql_fetch_assoc() is equivalent\n * to calling mssql_fetch_array() with MSSQL_ASSOC for the optional second\n * parameter.\n *\n * @result     mixed   The result resource that is being evaluated. This\n *                     result comes from a call to mssql_query().\n *\n * @return     mixed   Returns an associative array that corresponds to the\n *                     fetched row, or FALSE if there are no more rows.\n */", 
"mssql_free_result", T(Variant), S(0), "result", T(Variant), "N;", S(2), "null", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.mssql-free-result.php )\n *\n * mssql_free_result() only needs to be called if you are worried about\n * using too much memory while your script is running. All result memory\n * will automatically be freed when the script ends. You may call\n * mssql_free_result() with the result identifier as an argument and the\n * associated result memory will be freed.\n *\n * @result     mixed   The result resource that is being freed. This result\n *                     comes from a call to mssql_query().\n *\n * @return     mixed   Returns TRUE on success or FALSE on failure.\n */", 
"mssql_get_last_message", T(Variant), S(0), NULL, S(16384), "/**\n * ( excerpt from\n * http://php.net/manual/en/function.mssql-get-last-message.php )\n *\n * Gets the last message from the MS-SQL server\n *\n * @return     mixed   Returns last error message from server, or an empty\n *                     string if no error messages are returned from MSSQL.\n */", 

#elif EXT_TYPE == 1

#elif EXT_TYPE == 2
"MssqlResult", "", NULL, "__construct", T(Void), S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/mssqlresult.construct.php )\n *\n *\n */", S(16384),"__toString", T(String), S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/mssqlresult.tostring.php )\n *\n *\n * @return     string\n */", S(16384),NULL,NULL,NULL,
S(16384), "/**\n * ( excerpt from http://php.net/manual/en/class.mssqlresult.php )\n *\n * create a class of MssqlResult\n *\n */", 
#endif
